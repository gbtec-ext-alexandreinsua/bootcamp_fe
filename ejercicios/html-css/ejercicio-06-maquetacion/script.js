const alerta = [
    {
        mensaje: "El nombre del usuario no puede estar en blanco"
    }
];

const $banner = document.querySelector(".banner");

const fragment = document.createDocumentFragment();

